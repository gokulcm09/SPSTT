CREATE TABLE booking2021 (
  booking_id INTEGER NOT NULL AUTO_INCREMENT,
  name VARCHAR(255),
  available_time VARCHAR(255),
  booking_date VARCHAR(255),
  address VARCHAR(255),
  phone_number VARCHAR(255),
  mandapam VARCHAR(255),
  PRIMARY KEY(booking_id),
  UNIQUE(phone_number),
  UNIQUE(booking_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE booking2022 (
  booking_id INTEGER NOT NULL AUTO_INCREMENT,
  name VARCHAR(255),
  available_time VARCHAR(255),
  booking_date VARCHAR(255),
  address VARCHAR(255),
  phone_number VARCHAR(255),
  mandapam VARCHAR(255),
  PRIMARY KEY(booking_id),
  UNIQUE(phone_number),
  UNIQUE(booking_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE booking2023 (
  booking_id INTEGER NOT NULL AUTO_INCREMENT,
  name VARCHAR(255),
  available_time VARCHAR(255),
  booking_date VARCHAR(255),
  address VARCHAR(255),
  phone_number VARCHAR(255),
  mandapam VARCHAR(255),
  PRIMARY KEY(booking_id),
  UNIQUE(phone_number),
  UNIQUE(booking_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE booking2024 (
  booking_id INTEGER NOT NULL AUTO_INCREMENT,
  name VARCHAR(255),
  available_time VARCHAR(255),
  booking_date VARCHAR(255),
  address VARCHAR(255),
  phone_number VARCHAR(255),
  mandapam VARCHAR(255),
  PRIMARY KEY(booking_id),
  UNIQUE(phone_number),
  UNIQUE(booking_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

CREATE TABLE booking2025 (
  booking_id INTEGER NOT NULL AUTO_INCREMENT,
  name VARCHAR(255),
  available_time VARCHAR(255),
  booking_date VARCHAR(255),
  address VARCHAR(255),
  phone_number VARCHAR(255),
  mandapam VARCHAR(255),
  PRIMARY KEY(booking_id),
  UNIQUE(phone_number),
  UNIQUE(booking_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;